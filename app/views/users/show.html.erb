<h4>Welcome <%= @user.first_name %></h4>

<% if @current_user == @user %>

<div class="container">
    <div class="row">
        <div class="col-lg-3 col-lg-offset-3">
            <div class='profile_item' class="text-center">
                <%=link_to 'Contacts', contacts_path  %>
            </div>
        </div>
        <div class='profile_paths_container'>
            <div class='profile_item' class="text-center">
                <%=link_to 'New Search', contacts_path  %>
            </div>
        </div>
    </div>
    <div class="row">
        <div class="col-lg-3 col-lg-offset-3">
            <div class='profile_item' class="text-center">
                <%=link_to 'Existing Orders', 'contacts#index'  %>
            </div>
        </div>
        <div class='profile_paths_container'>
            <div class='profile_item' class="text-center">
                <%= form_tag do %>

                <% end %>
                <%=link_to 'Settings', edit_user_path(@user) %>
            </div>
        </div>
    </div>
</div>

<% end %>
